# Copy this to .env and fill in your actual values
# IMPORTANT: .env is in .gitignore - NEVER commit it to GitHub!

# AWS S3 Configuration
# Get these from your AWS S3 bucket
S3_BUCKET=your-bucket-name-here
AWS_REGION=us-east-1

# AWS Credentials (optional if using IAM role)
# Get these from AWS IAM → Users → Security Credentials
# WARNING: Treat like passwords - rotate periodically!
AWS_ACCESS_KEY_ID=your-access-key-id-here
AWS_SECRET_ACCESS_KEY=your-secret-access-key-here

# Session Secret (for cookie encryption)
# Generate random string: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=generate-a-random-secret-key-for-production
